Type Pokemon {
    _id: ID,
    name: String,
    pokenum: Int,
    height: Int,
    weight: Float,
    color: String,
    # Trainer: TrainerType,
    # Location: String,
    species: [String]
    talents: [Talent],
    description: String,
    sprite: String
}

extend type Pokemon {
  """
  Evolutions of this pokemon.
  """
  Evolutions: [Pokemon]
}

extend type Pokemon {
  """
  The type of the pokemon.
  """
  Type: PokeType
}


Type Trainer {
    _id: ID
    name: String,
    type: String
    height: Int,
    weight: Float,
    hairColor : String
}
Type Talent {
    _id: ID
    name: String,
    description: String
}

enum PokemonType {
  NORMAL
  FIRE
  WATER
  ELECTRIC
  GRASS
  ICE
  FIGHTING
  POISON
  GROUND
  FLYING
  PSYCHIC
  BUG
  ROCK
  GHOST
  DRAGON
  DARK
  STEEL
  FAIRY
}

enum AbilityType {
  PHYSIC
  SPECIAL
  STATUS
}

Type Ability {
    id: ID
    type: PokeType,
    ability: AbilityType,
    power: Int,
    precision: Float
    powerPoint: Int,
    target: Int
}

extend type Ability {
  """
  The type of the ability.
  """
  Type: PokeType
}